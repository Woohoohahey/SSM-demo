<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itactic.kaiy.ssm.dao.RoleMapper">

    <select id="findRolesByUsername" resultType="com.itactic.kaiy.ssm.entity.Role">
        select * from ssm_role a left join ssm_user_role b on a.rid=b.rid and
        b.uid in(SELECT uid from ssm_user where username=#{username})
    </select>
</mapper>
